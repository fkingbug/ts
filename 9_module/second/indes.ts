export const a = 5

export interface B {
  c: number
}

export const obj = {}

export interface B {
  c: number
}
export type typesmy = string | number

export default function aaa() {
  return ''
}
// export default type aaasss = number error

// type и interface вырезаются после компиляции
//default export может быть только 1
